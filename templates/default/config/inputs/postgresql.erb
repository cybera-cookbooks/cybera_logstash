  file {
    type => "postgresql"
    path => ["/var/log/postgresql/postgresql-*-main.log*"]
    exclude => ["*.gz"]
    codec => multiline {
      pattern => "(DETAIL:)|(STATEMENT:)"
      what => "previous"
    }
  }
