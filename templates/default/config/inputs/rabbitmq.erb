  rabbitmq {
    ack => true
    host => '<%= @host %>'
    port => <%= @port %>
    exchange => "logstash"
    user => "<%= @user %>"
    password => "<%= @password %>"
    ssl => <%=  @ssl_enabled %>
  }
