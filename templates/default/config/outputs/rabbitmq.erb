  rabbitmq {
    host => "<%= @host %>"
    port => <%= @port %>
    exchange => "logstash"
    user => "<%= @user %>"
    password => "<%= @password %>"
    exchange_type => "fanout"
    ssl => <%=  @ssl_enabled %>
  }
