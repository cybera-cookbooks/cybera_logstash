  rabbitmq {
    host => "<%= @host %>"
    port => <%= @port %>
    exchange => "logstash"
    user => "<%= @user %>"
    password => "<%= @password %>"
    ssl => <%=  @ssl_enabled %>
    durable => true
    exchange_type => "direct"
    persistent => true
  }
